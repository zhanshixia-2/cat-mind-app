---
description: 绝不在用户已改过的代码上整文件覆盖；新功能以最小差量合并
alwaysApply: true
---

# 保护用户已改动的代码

## 必须遵守

1. **在修改任何已存在的源文件前**，用 Read / diff 查看**工作区中当前内容**，以用户**最新保存的版本**为准，不以对话摘要或旧记忆为准。
2. **禁止**为「加功能」而用整份文件、整段大块替换、或从记忆中重贴「完整组件」，从而把用户**已手动改过的**文案、样式、顺序、命名或行为改回旧样。
3. 新功能应**局部修改**：`apply_patch` / 小范围 `search_replace`；在现有结构上**追加** import、state、JSX 分支或路由，而不是重写整文件。
4. 若需动到用户刚改过的同一区域，**先说明**将改哪几行、改什么，或先问用户**是否先提交/备份**当前改动，避免静默覆盖。
5. 若不确定某段是否用户手改，**先读文件再动**，或只改明确相关的最小范围。

## 反例（禁止）

- 「顺便」把同文件的格式、 className、文案恢复成项目里的「默认样例」
- 因合并冲突或图省事，用模板文件**整体替换**用户文件

## 正例

- 只加新路由、新组件、新 import，不动无关行
- 用户改过的 `App.css` 只追加新 class，不改用户动过的选择器
