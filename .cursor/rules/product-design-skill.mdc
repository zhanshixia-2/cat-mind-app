---
description: 新页面或新功能设计前必读 skills/design.md
alwaysApply: true
---

# 产品 / 界面设计前必读

在**规划、设计或实现**任何**新页面、新功能、较大 UI 改动**时，在动手写具体代码前必须：

1. 使用 **Read** 工具读取项目中的 `skills/design.md` 全文（若你尚未读过本轮会话，或该文件有更新，则重新阅读）。
2. 按 `skills/design.md` 中的约定做需求拆解、布局与实现；若与现有代码习惯冲突，以 `skills/design.md` 为准，并在回复中说明取舍。
3. 若 `skills/design.md` 不存在、为空、或仅含占位说明，先提醒用户将正式规范写入该文件后再继续细化设计，除非用户明确允许在无规范情况下自由发挥。

**路径**：`skills/design.md`（仓库根目录下 `skills` 文件夹）。
